dim(s) para extraer las dimensiones del dataframe., en este caso 12 filas y 2 columnas
PARA ORDENAR SATELITES.TXT VAMOS A HACER so=s[order(s$Radio),]
Le pongo un igual para ver que la asiganción se puede hacer de otra forma. Cda una de las posiciones entre corchetes separado por comas es las filas y las columnas
Lo que le digo dejando las columnasvacías en el comando es que la función se aplique solo ara las filas, que las columnas me dan igual. Me va a ordenar los datos
por filas y por radio. Order lo va a aplicar sobre la variable radio, le pongo el dolar porque la variuable está dentro del dataframe s. La variable que tiene que ordenar estádentro
del dataframe de s, y concretamente la varable radio indicandolo con un dolar
Para ordenar en el otro sentido pongo unrev delante. so=s[rev(order(s$Distancia)),] (el profe lo llevaba con radio pero yo lo tenia como distancia)

La funcion length length(s$Distancia) me saca cuantas filas hay de ese parámetro. Dim me lo devuelve en general, length por valor de variable
En R no se puede cacular el rango pero podemos hacer una simple resta entre el mayor y el menor elemento de un parámetro determinado rango = max(s$Distancia)-min(s$Distancia)

range(s$Distancia)
[1] 13 42
Esto no me da el escalar, me da el valor maximo y el minimo
Vamos a hacer una función de rango para tenerla. Vamos a hacer nuestra primera función.

Radio = s$Distancia que ya estoy cansao de poner el dolar
 rango = function(Radio){max(Radio)-min(Radio)} No se asigna variable, estoy llamando a la funcion rango
 Ahora la llamo
 rango(Radio)
[1] 29
Me he definido la función rango y me va a dar el resultado que quiero, pero, quiero cargarla en siguientes ejecuciones de R (porque si lo cierro se me pierde)
 dump("rango",file="rango.R") Es un .R porque es un programa, una línea pero es un programa. No es ni rhistory ni rnsq
 source("rango.R")
 Así cargamos el programa y lo tenemos de nuevo
 
 fracabsradio<-table(s$Distancia)
 Así calculo la frecuencia absoluta
 
  fracabsacumradio<-cumsum(fracabsradio)
  Así calculo la frecuencia absoluta acumulada
  
  No hay función de la frecuencia relativa
  
  frecrel <- function(Radio) {table(Radio)/length(Radio)}
  Me he definido una función para la frecuencia relativa
  
  > frecrelacum = function(Radio) {cumsum(table(Radio))/length(Radio)}
  Me he definido una función para la frecuencia relativa acumulada
  
  No tengo nada para la moda
  
  La media mr<-mean(Radio)
  
  La desviaciónn No sale lo msimo que en teoría porque esta la hace con n-1 para hacer inferencia estadística, no descriptiva. Nositros no necesitamis pensar en el sesgo, por eso en teoría la hemos hecho con n
  
  sdr<-sd(Radio)
  
  sdr2=sqrt((sdr^2)*(length(Radio)-1)/length(Radio)) Así lo podemos corregir
  
  > varr = varr*(length(Radio)-1) / length(Radio)
> varr
[1] 71.90972

La mediana y loc suartiles tampoco están hechos como en teoría
> medianr=median(Radio)
> medianr
[1] 24.5

cuar1=quantile(Radio, 0.25). No sale como en teoría porque usa distribuciones de probabilidad

R y Latex
rnwfile<-system.file("Sweave", "example-1.Rnw", package="utils") cargamos un rnw ya hecho
Sweave(rnwfile)  se genera el documento latex
tools::texi2pdf("example-1.tex")  se llama a miktex el cual compila el .tex y genera el pdf

